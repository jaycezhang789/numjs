{
  "name": "@jayce789/numjs-wasm",
  "private": true,
  "scripts": {
    "build": "cross-env RUSTFLAGS=-Ctarget-feature=+reference-types wasm-pack build --release --target web --out-name num_rs_wasm --out-dir pkg"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "wasm-pack": "^0.13.1"
  }
}
