[package]
name = "num_rs_wasm"
version = "0.1.0"
edition = "2021"


[lib]
crate-type = ["cdylib"]


[dependencies]
wasm-bindgen = "=0.2.84"
ndarray = "0.15"
num_rs_core = { path = "../core-rs", default-features = false, features = ["linalg"] }

[package.metadata.wasm-pack.profile.release]
wasm-opt = false
